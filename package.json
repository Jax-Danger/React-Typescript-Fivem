{
  "name": "fivem-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "node build.js",
    "build:obf": "node build.js --obfuscate",
    "build:watch": "node build.js --watch",

		"postinstall": "node newResource.js resourceA && node newResource.js resourceB"
  },
  "devDependencies": {
    "chokidar": "^4.0.3",
    "esbuild": "^0.19.0",
    "glob": "^10.3.3",
    "javascript-obfuscator": "^4.0.0"
  },
  "dependencies": {
    "@citizenfx/client": "^2.0.15015-1",
    "@citizenfx/server": "^2.0.14862-1"
  }
}
